/** @type {import('next').NextConfig} */
const nextConfig = {
    basePath: '/MyPage',
    assetPrefix: '/MyPage/',
    reactStrictMode: true,
    swcMinify: true,
  };
  
  module.exports = nextConfig;