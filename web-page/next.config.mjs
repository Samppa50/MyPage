/** @type {import('next').NextConfig} */
const nextConfig = {
    basePath: '/MyPage',
    assetPrefix: '/MyPage/',
    distDir: 'dist',
    reactStrictMode: true,
    swcMinify: true,
  };
  
export default nextConfig;